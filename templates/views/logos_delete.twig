{% extends 'layout.twig' %}

{% set module = 'Logos - Delete' %}
{% set nav = 'others' %}

{% block content %}
{% include 'slots/back-to-dashboard.twig' %}
<div id="logos_delete">
    <div class="row-fluid">
        <div class="span12">
            <div class="page-header">
                <h1>{{ module }}</h1>
            </div>
            <form accept-charset="UTF-8" action="{{ url('logos_delete', {'file_name': file_name}) }}" method="POST">
                <p>Are you sure you want to delete this logo?</p>
                <div class="form-actions">
                    <button class="btn btn-success" type="submit">
                        <span class="icon-info-sign icon-large"></span>
                        Yes
                    </button>
                    <a class="btn btn-danger" href="{{ url('logos_overview') }}">
                        <span class="icon-trash icon-large"></span>
                        No
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
